name: Docker CI + Sync to Hugging Face

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build Docker Image (Local Only)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Docker Image (No Push)
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./Dockerfile
          push: false
          tags: kamaleshbaskaran/student-score-prediction:latest

  sync-to-hub:
    name: Sync to Hugging Face Hub
    runs-on: ubuntu-latest
    needs: build  # Wait until Docker build completes
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to Hugging Face Hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git remote set-url origin https://huggingface.co/spaces/Kamaleshbaskaran/student-score-prediction
          git push origin main
